.Data
	const NUM 9
	INPUT[2]
.Code
procstart main
start:
	mov ax, 0
	mov bx, 1
	int 1

	mov ax, 0[0]
	mov bx, 0[1]
	
	cmp ax, 1
	jeq left_start
	cmp bx, 1
	jeq right_start
stop:
	mov ax, 0
	mov bx, 0
	int 0
	jmp start
left_start:
	mov ax, 1
	mov bx, 1000
	int 0
	jmp start
right_start:
	mov ax, 1
	mov bx, 1000
	int 0
	jmp start

procend main